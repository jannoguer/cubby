[Unit]
Description=Mutagen daemon
Documentation=https://mutagen.io/documentation/introduction/daemon

[Service]
Type=exec
# systemd resolves this against its own PATH, not your shell's; the README substitutes the full path.
ExecStart=mutagen daemon run
Restart=always
RestartSec=5

[Install]
WantedBy=default.target
